{
    "$schema": "https://opencode.ai/config.json",
    "mcp": {
        "figma": {
            "type": "local",
            "command": [
                "npx",
                "-y",
                "your-figma-mcp-server"
            ],
            "environment": {
                "FIGMA_API_KEY": "${FIGMA_API_KEY}"
            },
            "enabled": true
        }
    }
}